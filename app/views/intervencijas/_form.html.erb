<!DOCTYPE html>
<html>
  <head>
    <title>New intervencija</title>
    <!-- Any other tags you need (like csrf_meta_tags, csp_meta_tag, etc.) can go here. -->

    <!-- Inline CSS block -->
    <style>
      /* Container for the form */
      .form-container {
        max-width: 600px;
        margin: 2rem auto; /* center the form, add top/bottom margin */
        padding: 1rem;
      }

      /* Error explanation box */
      #error_explanation {
        background-color: #fef2f2; /* light red background */
        color: #dc2626;           /* red text */
        padding: 1rem;
        font-weight: 500;
        border-radius: 0.375rem;
        margin-top: 1rem;
      }

      /* Spacing for form groups */
      .field-group {
        margin: 1.25rem 0;
      }

      /* Label styling */
      label {
        display: block;
        font-weight: 600;
        margin-bottom: 0.5rem;
      }

      /* Input, textarea, and file field styling */
      input[type="text"],
      input[type="datetime-local"],
      textarea,
      input[type="file"] {
        display: block;
        width: 100%;
        padding: 0.75rem;
        border: 1px solid #d1d5db; /* gray border */
        border-radius: 0.375rem;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
        transition: border-color 0.2s ease;
        margin-top: 0.5rem;
      }
      input[type="text"]:focus,
      input[type="datetime-local"]:focus,
      textarea:focus,
      input[type="file"]:focus {
        outline: none;
        border-color: #2563eb; /* blue border on focus */
      }

      /* Submit button styling */
      .submit-button {
        background-color: #2563eb;
        color: #fff;
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: 0.375rem;
        font-weight: 600;
        cursor: pointer;
        transition: background-color 0.2s ease;
        margin-top: 1rem;
      }
      .submit-button:hover {
        background-color: #1e40af;
      }
    </style>
  </head>

  <body>
    <h1>New intervencija</h1>

    <div class="form-container">
      <!-- Begin the form -->
      <%= form_with(model: intervencija) do |form| %>
        <% if intervencija.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(intervencija.errors.count, "error") %> prohibited this intervencija from being saved:</h2>
            <ul>
              <% intervencija.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <!-- Datum Field -->
        <div class="field-group">
          <%= form.label :datum %>
          <%= form.datetime_field :datum %>
        </div>

        <!-- Lokacija Field -->
        <div class="field-group">
          <%= form.label :lokacija %>
          <%= form.text_field :lokacija %>
        </div>

        <!-- Tip Field -->
        <div class="field-group">
          <%= form.label :tip %>
          <%= form.text_field :tip %>
        </div>

        <!-- Kratek opis Field -->
        <div class="field-group">
          <%= form.label :kratek_opis %>
          <%= form.text_area :kratek_opis, rows: 4 %>
        </div>

        <!-- Podrobnejsi opis Field -->
        <div class="field-group">
          <%= form.label :podrobnejsi_opis %>
          <%= form.text_area :podrobnejsi_opis, rows: 4 %>
        </div>

        <!-- Images Field -->
        <div class="field-group">
          <%= form.label :images, "Attach Images" %>
          <%= form.file_field :images, multiple: true %>
        </div>

        <!-- Submit Button -->
        <%= form.submit "Create Intervencija", class: "submit-button" %>
      <% end %>
    </div>
  </body>
</html>
