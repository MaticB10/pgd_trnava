<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Showing Novica</title>
    <style>
      /* Container styling */
      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 1rem;
      }
      
      /* Notice message styling */
      .notice {
        background-color: #f0fdf4;
        color: #10b981;
        padding: 0.75rem 1rem;
        border-radius: 0.375rem;
        margin-bottom: 1rem;
        font-weight: 500;
      }
      
      /* Heading styling */
      h1 {
        font-size: 2.25rem; /* text-4xl equivalent */
        font-weight: 700;
        margin-bottom: 1.5rem;
      }
      
      /* Action buttons styling */
      .actions a,
      .actions form button {
        display: inline-block;
        padding: 0.75rem 1.5rem;
        border-radius: 0.375rem;
        font-weight: 600;
        text-align: center;
        text-decoration: none;
        margin-right: 0.5rem;
        margin-bottom: 0.5rem;
      }
      
      .btn-gray {
        background-color: #f3f4f6;
        color: #374151;
        border: 1px solid #d1d5db;
        transition: background-color 0.2s;
      }
      
      .btn-gray:hover {
        background-color: #e5e7eb;
      }
      
      .btn-red {
        background-color: #dc2626;
        color: #ffffff;
        border: none;
        transition: background-color 0.2s;
      }
      
      .btn-red:hover {
        background-color: #b91c1c;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <% if notice.present? %>
        <p class="notice"><%= notice %></p>
      <% end %>

      <h1>Showing Novica</h1>

      <div class="novica-content">
        <%= render partial: "novica", locals: { novica: @novica, show_images: true } %>
      </div>

      <div class="actions">
        <%= link_to "Back to novicas", novicas_path, class: "btn-gray" %>
      </div>
    </div>
  </body>
</html>
