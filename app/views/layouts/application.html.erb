<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "PGD Trnava" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Ikone, favicons, ipd. -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Glavni CSS in JavaScript -->
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <!-- Glavni body z osnovnim sivo-belim ozadjem -->
  <body class="flex flex-col min-h-screen bg-gray-100 text-gray-800">
    <!-- Glava (Header) -->
    <header class="bg-gray-800 text-white">
      <div class="container mx-auto px-6 py-4 flex items-center justify-between">
        <!-- Logotip / Naziv PGD -->
        <%= link_to "PGD Trnava", root_path, class: "text-2xl font-bold hover:text-gray-200" %>

        <!-- Navigacija -->
        <nav>
          <ul class="flex space-x-4">
            <li><%= link_to "Domov", root_path, class: "hover:text-gray-300" %></li>
            <li><%= link_to "Novice", novicas_path, class: "hover:text-gray-300" %></li>
            <li><%= link_to "Intervencije", intervencijas_path, class: "hover:text-gray-300" %></li>
            <li><%= link_to "Galerija", galerijas_path, class: "hover:text-gray-300" %></li>
            <% if user_signed_in? %>
              <li>
                <!-- Odjava preko button_to -->
                <%= button_to "Odjava", destroy_user_session_path,
                  method: :delete,
                  data: { confirm: "Ste prepričani?" },
                  class: "bg-red-600 hover:bg-red-700 transition-colors text-white px-4 py-2 rounded" %>
              </li>
            <% else %>
              <li><%= link_to "Prijava", new_user_session_path, class: "hover:text-gray-300" %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>

    <!-- Hero sekcija (opcijsko) -->
    <!-- Če ne želite hero sekcije, jo lahko izbrišete ali prilagodite -->
    <section class="relative bg-cover bg-center h-64" style="background-image: url('/assets/firefighter-bg.jpg');">
      <div class="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center">
        <h1 class="text-3xl md:text-4xl font-bold text-white">Dobrodošli v PGD Trnava</h1>
      </div>
    </section>

    <!-- Glavni vsebinski del (Main) -->
    <main class="container mx-auto flex-grow px-6 py-8">
      <%= yield %>
    </main>

    <!-- Noga (Footer) -->
    <footer class="bg-gray-900 text-white py-4">
      <div class="container mx-auto px-6 text-center">
        <p class="text-sm">&copy; <%= Time.current.year %> Prostovoljno gasilsko društvo Trnava. Vse pravice pridržane.</p>
      </div>
    </footer>
  </body>
</html>
